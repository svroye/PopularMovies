<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    tools:context="com.example.steven.popularmovies.MainActivity">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/mainActivity_recyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:id="@+id/mainActivity_progressBar"/>

    <TextView
        android:textAlignment="center"
        android:padding="8dp"
        android:id="@+id/mainActivity_noInternetOrErrorTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textColor="@android:color/white"
        tools:text="@string/no_internet_message"
        android:textSize="16sp"
        android:visibility="gone"/>

</RelativeLayout>
